<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.mapper.GoodsMapepr">



    <select id="getqueryPage" resultType="com.jk.bean.Goods">
        select rer.* from(
        select re.*,rownum rn from (
        select Id,shp_mch,shp_tp,flbh1,flbh2,pp_id,chjshj,shp_msh
        from t_mall_product
        )re where rownum &lt;= #{end}

        ) rer where rer.rn>#{st}
    </select>



    <select id="getqueryTotal" resultType="long">
        select count(*) from t_mall_product

    </select>


</mapper>